# cordova engine

This directory documents the cordova generator. Runtime project files are generated by `scripts/build.mjs` into `work/<build-id>/project` so each build starts clean and does not inherit stale Gradle or platform state.
